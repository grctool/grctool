input_gates:
  - criteria: All test suites written and reviewed
    required: true
    source: 03-test
    validation: Tests cover all requirements and edge cases
  - criteria: Tests are failing (Red phase of TDD)
    required: true
    source: 03-test
    validation: Tests run but fail without implementation
  - criteria: Test coverage plan approved
    required: true
    source: 03-test
    validation: Coverage targets defined for unit, integration, and e2e
  - criteria: CI/CD pipeline configured for tests
    required: true
    source: 03-test
    validation: Automated test execution on commits